name: Release mac app

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    env:
      MORN_RELEASE_TOKEN: ${{ secrets.MORN_RELEASE_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: VERSION="${GITHUB_REF_NAME#v}" UNIVERSAL=1 zsh build.sh

      - name: Sign & notarize via MornNotary
        env:
          GH_TOKEN: ${{ secrets.MORN_RELEASE_TOKEN }}
        run: |
          set -euo pipefail
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::error::MORN_RELEASE_TOKEN secret is not set. Register a fine-grained PAT covering matsufriends/MornNotary and matsufriends/homebrew-tap (Contents: read/write, Actions: read) as MORN_RELEASE_TOKEN."
            exit 1
          fi
          VERSION="${GITHUB_REF_NAME#v}"

          SUBMIT_ZIP="$(mktemp -d)/MornDesktopTube.zip"
          ditto -c -k --keepParent dist/MornDesktopTube.app "$SUBMIT_ZIP"

          WORKDIR="$(mktemp -d)"
          git clone --depth 1 "https://x-access-token:${GH_TOKEN}@github.com/matsufriends/MornNotary.git" "$WORKDIR"
          cp "$SUBMIT_ZIP" "$WORKDIR/incoming/MornDesktopTube.zip"
          cd "$WORKDIR"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add incoming/MornDesktopTube.zip
          git commit -m "request: sign MornDesktopTube v${VERSION}"
          git push
          SHA="$(git rev-parse HEAD)"

          RUN_ID=""
          for i in $(seq 1 30); do
            RUN_ID=$(gh run list --repo matsufriends/MornNotary --workflow sign.yml \
              --json databaseId,headSha --jq ".[] | select(.headSha==\"$SHA\") | .databaseId" | head -1)
            [ -n "$RUN_ID" ] && break
            sleep 5
          done
          if [ -z "$RUN_ID" ]; then
            echo "::error::MornNotary workflow run did not start for commit $SHA"
            exit 1
          fi
          echo "MornNotary run: $RUN_ID"
          gh run watch "$RUN_ID" --repo matsufriends/MornNotary --exit-status

          OUTDIR="$(mktemp -d)"
          gh run download "$RUN_ID" --repo matsufriends/MornNotary --name "MornDesktopTube-signed" --dir "$OUTDIR"
          SIGNED_ZIP="$(find "$OUTDIR" -name '*.zip' | head -1)"
          if [ -z "$SIGNED_ZIP" ]; then
            echo "::error::Signed artifact not found in MornNotary run output"
            exit 1
          fi

          cd "$GITHUB_WORKSPACE"
          mv dist/MornDesktopTube.app "$OUTDIR/unsigned.app"
          ditto -x -k "$SIGNED_ZIP" dist
          codesign --verify --deep --strict --verbose=2 dist/MornDesktopTube.app
          xcrun stapler validate dist/MornDesktopTube.app

      - name: Zip app
        run: |
          cd dist
          ditto -c -k --keepParent MornDesktopTube.app MornDesktopTube.app.zip

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          if gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$TAG" dist/MornDesktopTube.app.zip --repo "$GITHUB_REPOSITORY" --clobber
          else
            gh release create "$TAG" dist/MornDesktopTube.app.zip --repo "$GITHUB_REPOSITORY" --generate-notes
          fi

      - name: Update Homebrew tap
        if: ${{ env.MORN_RELEASE_TOKEN != '' }}
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          SHA="$(shasum -a 256 dist/MornDesktopTube.app.zip | awk '{print $1}')"
          git clone "https://x-access-token:${MORN_RELEASE_TOKEN}@github.com/matsufriends/homebrew-tap.git" tap
          mkdir -p tap/Casks
          sed -e "s/@VERSION@/${VERSION}/g" -e "s/@SHA256@/${SHA}/g" Support/morndesktoptube.rb.in > tap/Casks/morndesktoptube.rb
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/morndesktoptube.rb
          git diff --cached --quiet || { git commit -m "morndesktoptube ${VERSION}"; git push; }
